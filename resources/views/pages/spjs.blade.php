<x-app-shell title="{{ auth()->user()->isBendahara() ? 'Semua SPJ' : 'SPJ Saya' }}" description="Daftar surat pertanggungjawaban.">
	<div class="page-head"><div><p class="eyebrow">Arsip pertanggungjawaban</p><h1>{{ auth()->user()->isBendahara() ? 'Semua SPJ' : 'SPJ Saya' }}</h1><p class="head-copy">Pantau status pengajuan dan keputusan bendahara.</p></div>@if (! auth()->user()->isBendahara())<a class="review-button" href="{{ route('spj.create') }}">+ Ajukan SPJ</a>@endif</div>
	@if (session('status'))<div class="form-success">{{ session('status') }}</div>@endif
	<section class="panel queue"><div class="panel-header"><div><h2 class="panel-title">{{ $spjs->count() }} pengajuan</h2></div></div><div class="table-wrap"><table class="queue-table"><thead><tr><th>Nomor SPJ</th><th>Uraian</th><th>Unit</th><th>Nilai</th><th>Status</th></tr></thead><tbody>@forelse ($spjs as $spj)<tr><td>{{ $spj->number }}</td><td>{{ $spj->title }}</td><td>{{ $spj->unit_name }}</td><td class="amount">Rp{{ number_format((float) $spj->submitted_amount, 0, ',', '.') }}</td><td><span class="status {{ $spj->status === 'approved' ? 'ready' : ($spj->status === 'revision' ? 'revision' : 'review') }}">{{ $spj->status === 'approved' ? 'Disetujui' : ($spj->status === 'revision' ? 'Perlu revisi' : 'Menunggu review') }}</span></td></tr>@empty<tr><td colspan="5">Belum ada pengajuan SPJ.</td></tr>@endforelse</tbody></table></div></section>
</x-app-shell>